using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BeeController : InvaderController {

	// Use this for initialization
	void Start () {
        OnCreate();
    }
	
	// Update is called once per frame
	void Update () {
        //StartCoroutine(Move());
    }
}
